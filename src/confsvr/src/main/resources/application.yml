server:
   port: 8888
spring:
    profiles:
        active: native
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/SnakeDoc/SpringMicroservicesInActionConfig
                    searchLocations: licensingservice, organizationservice
                    